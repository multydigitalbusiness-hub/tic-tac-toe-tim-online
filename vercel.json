{
  "framework": "vite",
  "buildCommand": "pnpm --filter @ttt/shared build && pnpm --filter @ttt/web build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
