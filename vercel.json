{
  "framework": "vite",
  "buildCommand": "pnpm --filter @ttt/shared build && pnpm --filter @ttt/web build && cp -r apps/web/dist dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
